# Warcraft API

`common.j` and `common.ai` declare the Warcraft III types and native functions
used by the AI. Wurst uses these declarations for type checking; Warcraft
provides their implementations at runtime.

When the AI uses another native, copy its exact declaration from Warcraft's
official `common.j` or `common.ai`.
